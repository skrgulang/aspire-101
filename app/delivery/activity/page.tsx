import DeliveryActivityCenter from './DeliveryActivityCenter';

export const metadata = {
  title: 'Delivery Activity · Aspire 101',
  description: 'See delivery actions, offer updates, urgency, and protected-delivery follow-up in one place.'
};

export default function DeliveryActivityPage() {
  return <DeliveryActivityCenter />;
}
